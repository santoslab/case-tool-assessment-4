#include <all.h>

void base_Base_Types_Bits_example(STACK_FRAME IS_C4F575 result) {
  DeclNewStackFrame(caller, "Base_Types.scala", "base.Base_Types", "Bits_example", 0);
  STATIC_ASSERT(0 <= MaxIS_C4F575, "Insufficient maximum for IS[Z, B] elements.");
  DeclNewIS_C4F575(t_0);
  t_0.size = (int8_t) 0;
  Type_assign(result, (&t_0), sizeof(struct IS_C4F575));
  return;
}